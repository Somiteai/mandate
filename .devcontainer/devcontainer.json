{
  "name": "Mandate",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "mandate",
  "workspaceFolder": "/workspace",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=cached",
  "customizations": {
    "vscode": {
      "extensions": []
    }
  },
  "postCreateCommand": "git config --global --add safe.directory /workspace && ./scripts/setup-git-hooks.sh"
}
