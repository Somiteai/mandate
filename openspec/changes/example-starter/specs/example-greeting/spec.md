## ADDED Requirements

### Requirement: Greeting message

The system SHALL display a greeting when the example endpoint or page is viewed.

#### Scenario: Default greeting

- **WHEN** a user opens the example page
- **THEN** the UI shows the text "Hello from Mandate + OpenSpec"
