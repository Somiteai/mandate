## 1. Spec libraries

- [x] Document harness vs product vs `~/MDS/` in `docs/START.md` and `docs/harness-vs-project.md`
- [x] Point README at Mac environment and the split

## 2. macOS + container

- [x] Replace WSL-first `docs/dev-environment.md`
- [x] Add `.devcontainer/devcontainer.json`
- [x] Fix `init-project.sh` for Bash 3.2
- [x] Stop recommending host `npm i -g` for OpenSpec

## 3. Host (outside this repo)

- [x] Document `~/MDS/` layout; create the folder with a README
- [ ] Human: FileVault (done), `gh auth` (done), install Docker Desktop with password, reopen this folder in the container
